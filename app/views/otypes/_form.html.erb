<%= form_with(model: otype) do |form| %>
  <% if otype.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(otype.errors.count, "error") %> prohibited this otype from being saved:</h2>

      <ul>
        <% otype.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
