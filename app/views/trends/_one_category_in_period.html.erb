{
    label: '<%= one_category_in_period.shift %>',
    data: [
        <% one_category_in_period[0].each do |period| %>
            {
                x: '<%= @trend.period == "month" ? l( period[0], format: '%b') : period[0] %>',
                y: '<%= period[1] %>'
            },
        <% end %>
        
    ]
},